import { ComingSoon } from '@/components/ComingSoon';

export function UsersPage() {
  return (
    <ComingSoon
      title="Usuarios"
      description="Crear, editar, activar/desactivar y resetear contraseña — cajero, bodega y administrador."
      task="T-09"
    />
  );
}
